name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Rebrand APK Metadata Dynamically
        run: |
          RAW_NAME="${{ github.event.repository.name }}"
          APP_NAME="$RAW_NAME"
          CLEAN_PKG=$(echo "$RAW_NAME" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]//g')
          PACKAGE_NAME="com.${CLEAN_PKG}.app"

          STRINGS_FILE="android/app/src/main/res/values/strings.xml"
          if [ -f "$STRINGS_FILE" ]; then
            sed -i "s/<string name=\"app_name\">.*<\/string>/<string name=\"app_name\">$APP_NAME<\/string>/" "$STRINGS_FILE"
          fi

          GRADLE_FILE="android/app/build.gradle"
          if [ -f "$GRADLE_FILE" ]; then
            sed -i "s/applicationId \".*\"/applicationId \"$PACKAGE_NAME\"/" "$GRADLE_FILE"
          fi

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Execute Full JS & Native Codegen
        run: |
          # 1. Trigger CLI codegen to generate C++ headers from schema
          npx react-native codegen || true

          # 2. Trigger Gradle codegen task
          cd android
          ./gradlew :app:generateCodegenArtifactsFromSchema --no-daemon

          # 3. Create missing jni path inside node_modules package build directory
          TARGET_DIR="../node_modules/react-native-safe-area-context/android/build/generated/source/codegen/jni"
          mkdir -p "$TARGET_DIR"

          # 4. Copy all generated schema headers to safe-area-context target
          if [ -d "app/build/generated/source/codegen/jni" ]; then
            cp -r app/build/generated/source/codegen/jni/* "$TARGET_DIR"/
          fi

      - name: Build Release APK
        run: |
          cd android
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}-APK
          path: android/app/build/outputs/apk/release/*.apk
