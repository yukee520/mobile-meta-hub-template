rootProject.name = "MobileMetaHub"
include(':app')

def settingsAutoLinkScript = new File(settingsDir, "../node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
if (settingsAutoLinkScript.exists()) {
    apply from: settingsAutoLinkScript
    applyNativeModulesSettingsGradle(settings)
}
